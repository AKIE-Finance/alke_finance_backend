# --- Base de données -------------------------------------------------------
DATABASE_URL="postgresql://alke:alke_dev_local_only@localhost:5433/alke_finance_dev?schema=public"

# --- Redis (cache/session) --------------------------------------------------
REDIS_URL="redis://localhost:55380"

# --- Authentification --------------------------------------------------------
JWT_SECRET="change-me-in-production-use-a-long-random-string"
JWT_EXPIRES_IN="7d"

# --- Général ------------------------------------------------------------
PORT=3000
NODE_ENV=development

# --- OTP (mode démo tant qu'Africa's Talking n'est pas branché) -----------
OTP_DEMO_MODE=true
OTP_DEMO_CODE=123456

# --- Taux de change (gratuit, sans clé — https://www.exchangerate-api.com) -
FX_RATES_BASE_URL="https://open.er-api.com/v6/latest"

# --- Twelve Data (déjà utilisé côté app mobile, clé démo) ------------------
TWELVE_DATA_API_KEY=demo
TWELVE_DATA_BASE_URL="https://api.twelvedata.com"

# --- Fournisseurs externes (laisser vide -> mode simulé automatique) -------
MTN_MOMO_SUBSCRIPTION_KEY=
ORANGE_MONEY_CLIENT_ID=
CINETPAY_API_KEY=
SMILE_IDENTITY_API_KEY=
AFRICAS_TALKING_API_KEY=
SENDGRID_API_KEY=
SENTRY_DSN=
